% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cells.R
\name{xlsx_cells}
\alias{xlsx_cells}
\title{Read the cells of a worksheet}
\usage{
xlsx_cells(path, sheet = 1)
}
\arguments{
\item{path}{Path to an \code{.xlsx} file.}

\item{sheet}{The worksheet to read: either its name, or its position in the
workbook.}
}
\value{
A data frame with one row per cell and the columns:
\describe{
\item{\code{row}, \code{col}}{Position, counting from 1.}
\item{\code{type}}{One of \code{"blank"}, \code{"number"}, \code{"string"}, \code{"boolean"},
\code{"error"} or \code{"date"}.}
\item{\code{value}}{The cell as written, as a string.}
\item{\code{number}}{The numeric value for \code{"number"}, \code{"boolean"} and
\code{"date"} cells, and \code{NA} otherwise. A date is its Excel serial
number; it is not converted here, because the epoch is a property of
the workbook rather than of the cell.}
}
}
\description{
Reads a worksheet one cell at a time and returns them in a data frame, one
row per cell. This is the low-level reader: it reports what is in each cell
without assembling columns or guessing a type for them, which is what
\code{read_xlsx()} will do on top of it.
}
\examples{
path <- system.file("extdata", "two-sheets.xlsx", package = "zuxlsx")
cells <- xlsx_cells(path)
head(cells)

# The type is the cell's own, before any column is built from it. A date is
# reported as a date and its serial number given unconverted, because the
# epoch belongs to the workbook rather than to the cell.
table(cells$type)
attr(cells, "date1904")

# The second worksheet, by name rather than by position.
xlsx_cells(path, "notes")
}
\seealso{
\code{\link[=xlsx_sheets]{xlsx_sheets()}} to list the worksheets, and \link{zuxlsx-conditions}
for the errors this can raise.
}
