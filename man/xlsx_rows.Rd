% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rows.R
\name{xlsx_rows}
\alias{xlsx_rows}
\title{Read a worksheet as rows}
\usage{
xlsx_rows(path, sheet = 1)
}
\arguments{
\item{path}{Path to an \code{.xlsx} file.}

\item{sheet}{The worksheet to read: either its name, or its position in the
workbook.}
}
\value{
A list with one character vector per row, from the first row of the
worksheet to the last. Every vector is as long as the widest row, so the
nth element of each is the nth column. Blank cells are \code{NA}.
}
\description{
Returns a worksheet row by row, as the text of each cell, without inferring
a type for any column. This is the row-oriented counterpart to
\code{\link[=xlsx_cells]{xlsx_cells()}}, and is the shape to reach for when a worksheet is not
rectangular enough for \code{\link[=read_xlsx]{read_xlsx()}} -- a report with stacked blocks, or a
file whose header is several rows deep.
}
\examples{
path <- system.file("extdata", "two-sheets.xlsx", package = "zuxlsx")
if (nzchar(path)) xlsx_rows(path)
}
\seealso{
\code{\link[=xlsx_cells]{xlsx_cells()}} for typed cells, \code{\link[=read_xlsx]{read_xlsx()}} for a data frame.
}
